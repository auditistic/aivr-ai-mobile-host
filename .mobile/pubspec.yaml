name: aivr_farm_node
description: Dedicated AI Farm worker node — earn tokens by contributing mobile compute
publish_to: 'none'
version: 2.0.0+1

environment:
  sdk: '>=3.7.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cactus: ^1.3.0
  wakelock_plus: ^1.1.4
  path_provider: ^2.1.2
  uuid: ^4.3.3
  shared_preferences: ^2.2.2
  http: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

dependency_overrides:
  path: ^1.9.0
